local Window = Rayfield:CreateWindow({
   Name = "Mini Yushi Cheater",
   LoadingTitle = "Loading Mini Yushi Cheater",
   LoadingSubtitle = "by MYC Group",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = WindowMyc, 
      FileName = "MYC"
   },
   KeySystem = true, 
   KeySettings = {
      Title = "Key To Access the Menu",
      Subtitle = "The Key is in The Discord Server",
      Note = "dc.gg/myc",
      FileName = "KeyLock", 
      SaveKey = true, 
      GrabKeyFromSite = false, 
      Key = {"000"} 
   },
Discord = {
      Enabled = true,
      Invite = "https://dc.gg/myc",
      RememberJoins = true
})


--Creating a Tab
local MTab = Window:CreateTab("Main", 4483362458), 
local FTab = Window:CreateTab("Fun", 4483362458), 
local TTab = Window:CreateTab("Trolling", 4483362458), 
local STab = Window:CreateTab("Settings", 4483362458), 



Destroying the Interface
Rayfield:Destroy()

-- First notification  
Rayfield:Notify({  
    Title = "Mini Yushi Cheater",  
    Content = "Mini Yushi Cheater has successfully loaded.",  
    Duration = 6.5,  
})  

-- Wait for the duration of the first notification  
wait(6.5)  

-- Second notification  
Rayfield:Notify({  
    Title = "Important Information",  
    Content = "To close, click K.",  
    Duration = 5, -- Adjust the duration as needed  
})

local Button = TTab:CreateButton({  
    Name = "Fake Admin Team",  
    Callback = function()  
        -- Safely load and execute the Lua script from the specified URL  
        local success, err = pcall(function()  
            loadstring(game:HttpGet("https://raw.githubusercontent.com/game-hax/uca/main/release/uca.min.lua"))()  
        end)  
    end  
})

local Button = TTab:CreateButton({  
    Name = "Go Bonkers",  
    Callback = function()  
        -- Safely load and execute the Lua script from the specified URL  
        local TweenService = game:GetService("TweenService")
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local rootPart = character:WaitForChild("HumanoidRootPart")

local function createBonkersCFrame()
    local randomOffset = Vector3.new(math.random(-5, 5), math.random(5, 10), math.random(-5, 5))
    local randomRotation = math.rad(math.random(-180, 180))
    return rootPart.CFrame * CFrame.new(randomOffset) * CFrame.Angles(randomRotation, randomRotation, randomRotation)
end

local tweenInfo = TweenInfo.new(10, Enum.EasingStyle.Elastic, Enum.EasingDirection.InOut)
local bonkersCFrame = createBonkersCFrame()
local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = bonkersCFrame})

tween:Play()
        end)  
    end  
})


local Player = game.Players.LocalPlayer  
local Character = Player.Character or Player.CharacterAdded:Wait()  

local ColorPicker = Tab:CreateColorPicker({  
    Name = "Skin Changer",  
    Color = Color3.fromRGB(255, 255, 255),  
    Flag = "ColorPicker1",  
    Callback = function(Value)  
        -- The variable (Value) is a Color3 value based on which color is selected  

        -- Change the skin color  
        local bodyColors = Character:FindFirstChildOfClass("BodyColors")  

        if bodyColors then  
            -- Change the skin color to the selected color from the color picker  
            bodyColors.HeadColor = Value -- Change Head color  
            bodyColors.LeftArmColor = Value -- Change Left Arm color  
            bodyColors.RightArmColor = Value -- Change Right Arm color  
            bodyColors.LeftLegColor = Value -- Change Left Leg color  
            bodyColors.RightLegColor = Value -- Change Right Leg color  
    end  
})


--Creating an Adaptive Input (TextBox)
local Input = TTab:CreateInput({
   Name = "Bang Bang Tool 😏",
   PlaceholderText = "Name",
   RemoveTextAfterFocusLost = true,
   Callback = function(Text)
   -- The function that takes place when the input is changed
   -- The variable (Text) is a string for the value in the text box
   end,
})

local Toggle = TTab:CreateToggle({
   Name = "Bang",
   CurrentValue = false,
   Flag = "Toggle1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
   -- The function that takes place when the toggle is pressed
   -- The variable (Value) is a boolean on whether the toggle is true or false
   end
})

local Label = STab:CreateLabel("Credits : UI : Me , some Scripting : Christan my Friend , Bonkers Script : mov-ebx")
end
})